# Stripped down from https://github.com/dart-lang/pub/blob/master/.travis.yml

language: dart

dart:
  - dev

os:
  - linux
  - osx
  #- windows # Currently does not work for Windows

dart_task:
  - test: .
  - dartfmt
  - dartanalyzer: --fatal-infos --fatal-warnings .

cache:
  directories:
    - $HOME/.pub-cache